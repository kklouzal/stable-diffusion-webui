# GB10/A1111 maintained Python requirements.
# This is now the canonical direct dependency surface for the GB10 image and
# normal launch bootstrap. Do not list torch, torchvision, torchaudio, triton,
# CUDA, or NVIDIA packages here: the GB10 Docker image inherits and protects
# the selected NGC CUDA/PyTorch lane explicitly. torchaudio is optional-absent
# in that lane unless a real runtime import requirement is proven.
setuptools==69.5.1
GitPython
Pillow
pytz==2026.4
accelerate
blendmodes==2025
clean-fid
diskcache
einops
facexlib
fastapi==0.141.1
uvicorn==0.54.0
httpcore
httpx
requests
inflection
jsonmerge
kornia
lark
ninja
numpy
omegaconf
open-clip-torch==3.3.0
piexif
protobuf
psutil
pytorch_lightning
torchmetrics
lightning-utilities
safetensors
scikit-image
spandrel
spandrel-extra-arches
tomesd
torchdiffeq
torchsde
transformers
huggingface-hub>=1.13.0
tokenizers
pillow-avif-plugin
# pydantic is intentionally omitted: fastapi pulls it in, and it resolves as a released
# NGC package (docker/base-released-packages.txt) with the NGC version as its floor.
